# ========================================
# CC-Admin 环境变量配置模板文件
# ========================================
#
# 此文件是环境变量的模板文件，包含了项目中使用的所有环境变量的默认配置
# 使用方法：
# 1. 复制此文件为 .env.development（开发环境配置）
# 2. 复制此文件为 .env.production（生产环境配置）
# 3. 根据实际需要修改各环境的配置值
#
# 注意事项：
# - 只有以 VITE_ 开头的变量才会在客户端代码中可用
# - 环境变量值都是字符串类型，使用时需要适当转换
# - 敏感信息请放在 .env.local 文件中（此文件会被 git 忽略）
#
# 更多信息请查看：https://cn.vitejs.dev/guide/env-and-mode.html

# ===========================================
# 应用基础配置
# ===========================================

# 应用标题 - 显示在浏览器标题栏和页面中
VITE_APP_TITLE=CC-Admin

# 应用版本号 - 用于版本控制和显示，建议遵循语义化版本规范
VITE_APP_VERSION=1.0.0

# pinia key 前缀
VITE_PINIA_PERSIST_KEY_PREFIX=cc-admin-pinia-key


# 当前运行环境 - 可选值: development | production | test
# 用于区分不同的部署环境，影响功能开关和配置加载
VITE_APP_ENV=development

# ===========================================
# API 接口配置
# ===========================================

# API 服务器基础地址 - 所有 API 请求的根路径
# 开发环境: http://localhost:3003
# 生产环境: https://your-api-domain.com/api
VITE_API_BASE_URL=http://localhost:3003

# API 请求超时时间（毫秒）- 防止请求长时间无响应
# 建议开发环境设置较长时间，生产环境根据网络情况调整
VITE_API_TIMEOUT=30000

# ===========================================
# 开发环境配置
# ===========================================

# 是否启用开发者工具 - 控制 Vue DevTools 等开发工具的显示
# true: 启用开发工具（推荐开发环境）
# false: 禁用开发工具（推荐生产环境）
VITE_DEV_TOOLS=true

# 是否启用 Mock 数据 - 用于开发阶段模拟 API 响应
# true: 使用 Mock 数据（前端独立开发时使用）
# false: 使用真实 API（联调和生产环境使用）
VITE_MOCK_ENABLE=true

# 是否启用控制台日志 - 控制 console.log 等调试信息的输出
# true: 显示控制台日志（便于开发调试）
# false: 隐藏控制台日志（减少生产环境输出）
VITE_CONSOLE_LOG=true

# ===========================================
# 构建优化配置
# ===========================================

# 生产构建时是否移除 debugger 语句 - 提升生产环境性能
# true: 移除 debugger（推荐生产环境）
# false: 保留 debugger（推荐开发环境）
VITE_DROP_DEBUGGER=false

# 生产构建时是否移除 console 语句 - 减少生产包大小
# true: 移除 console（推荐生产环境）
# false: 保留 console（推荐开发环境）
VITE_DROP_CONSOLE=false

# 是否启用 Gzip 压缩 - 减少传输文件大小
# true: 启用压缩（推荐生产环境）
# false: 禁用压缩（开发环境可选）
VITE_BUILD_GZIP=false

# 是否启用构建分析 - 生成打包分析报告
# true: 生成分析报告（用于优化包大小）
# false: 不生成报告（常规构建）
VITE_BUILD_ANALYZE=false

# ===========================================
# 其他功能配置
# ===========================================

# 是否使用 Mock 服务 - 全局 Mock 开关（预留字段）
# 与 VITE_MOCK_ENABLE 类似，用于更细粒度的 Mock 控制
VITE_USE_MOCK=false
